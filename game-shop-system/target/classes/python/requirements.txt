# Python visualization service dependencies
pandas==2.2.2
SQLAlchemy==2.0.35
PyMySQL==1.1.1
pyecharts==2.0.5
# Prefer snapshot-selenium (Chrome/Chromium headless)
snapshot-selenium==0.0.3
selenium==4.24.0
pillow==10.4.0

# Optional fallback (PhantomJS is deprecated; install only if needed):
# snapshot-phantomjs
